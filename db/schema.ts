import { integer, sqliteTable, text, uniqueIndex } from 'drizzle-orm/sqlite-core';
export const members = sqliteTable('members', { id: text('id').primaryKey(), userId: text('user_id'), email: text('email').notNull(), name: text('name').notNull(), role: text('role').notNull(), createdAt: text('created_at').notNull() }, t => [uniqueIndex('members_email').on(t.email), uniqueIndex('members_user').on(t.userId)]);
export const packages = sqliteTable('packages', { id: text('id').primaryKey(), name: text('name').notNull(), subsystem: text('subsystem').notNull(), status: text('status').notNull().default('In progress'), version: integer('version').notNull().default(1), currentVersionId: text('current_version_id').notNull(), lockedBy: text('locked_by'), lockToken: text('lock_token'), lockedAt: text('locked_at'), updatedAt: text('updated_at').notNull() });
export const versions = sqliteTable('versions', { id: text('id').primaryKey(), packageId: text('package_id').notNull().references(() => packages.id), revision: integer('revision').notNull(), filename: text('filename').notNull(), size: integer('size').notNull(), storageKey: text('storage_key').notNull(), note: text('note').notNull(), authorId: text('author_id').notNull(), authorName: text('author_name').notNull(), createdAt: text('created_at').notNull() }, t => [uniqueIndex('versions_package_revision').on(t.packageId, t.revision)]);
export const activity = sqliteTable('activity', { id: text('id').primaryKey(), packageId: text('package_id'), actor: text('actor').notNull(), message: text('message').notNull(), createdAt: text('created_at').notNull() });
